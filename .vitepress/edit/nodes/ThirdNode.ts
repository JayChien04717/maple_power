import ThirdNodeView from './ThirdNodeView.vue'

export const ThirdNode = {} /* Node.create({
  name: 'third',

  group: 'block',

  content: 'block+',

  defining: true,

  isolating: true,

  addAttributes() {
    return {
      class: {
        default: 'custom-block third',
      },
    }
  },

  parseHTML() {
    return [
      {
        tag: 'div.custom-block.third',
      },
    ]
  },

  renderHTML({ HTMLAttributes }) {
    return ['div', mergeAttributes(HTMLAttributes, { class: 'custom-block third' }), 0]
  },

  addNodeView() {
    return VueNodeViewRenderer(ThirdNodeView)
  },
}) */
