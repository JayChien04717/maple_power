import HalfNodeView from './HalfNodeView.vue'

export const HalfNode = {} /* Node.create({
  name: 'half',

  group: 'block',

  content: 'block+',

  defining: true,

  isolating: true,

  addAttributes() {
    return {
      class: {
        default: 'custom-block half',
      },
    }
  },

  parseHTML() {
    return [
      {
        tag: 'div.custom-block.half',
      },
    ]
  },

  renderHTML({ HTMLAttributes }) {
    return ['div', mergeAttributes(HTMLAttributes, { class: 'custom-block half' }), 0]
  },

  addNodeView() {
    return VueNodeViewRenderer(HalfNodeView)
  },
}) */
